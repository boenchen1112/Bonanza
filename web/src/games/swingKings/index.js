/**
 * Swing Kings — PLACEHOLDER. Replace this whole file with the real minigame.
 * See ../../ARCHITECTURE.md for the minigame interface.
 */
import { makeStub } from '../_stub.js';

export default makeStub({
  id: 'swing-kings',
  name: 'Swing Kings',
  blurb: 'One swing. One beat. Send it.',
  bpm: 124,
  bars: 16,
  color: 0xffd93d,
});
