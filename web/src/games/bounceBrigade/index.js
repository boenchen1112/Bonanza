/**
 * Bounce Brigade — PLACEHOLDER. Replace this whole file with the real minigame.
 * See ../../ARCHITECTURE.md for the minigame interface.
 */
import { makeStub } from '../_stub.js';

export default makeStub({
  id: 'bounce-brigade',
  name: 'Bounce Brigade',
  blurb: 'Land every landing.',
  bpm: 118,
  bars: 16,
  color: 0x9ee87a,
});
