/**
 * Drumline Dash — PLACEHOLDER. Replace this whole file with the real minigame.
 * See ../../ARCHITECTURE.md for the minigame interface.
 */
import { makeStub } from '../_stub.js';

export default makeStub({
  id: 'drumline-dash',
  name: 'Drumline Dash',
  blurb: 'Hear it. Hit it back.',
  bpm: 132,
  bars: 16,
  color: 0x4dd6ff,
});
