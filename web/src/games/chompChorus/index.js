/**
 * Chomp Chorus — PLACEHOLDER. Replace this whole file with the real minigame.
 * See ../../ARCHITECTURE.md for the minigame interface.
 */
import { makeStub } from '../_stub.js';

export default makeStub({
  id: 'chomp-chorus',
  name: 'Chomp Chorus',
  blurb: 'Four lanes. One groove.',
  bpm: 140,
  bars: 16,
  color: 0xff5d73,
});
